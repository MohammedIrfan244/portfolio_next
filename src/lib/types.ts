export interface INavigation {
    id: string;
  name: string;
  value: string;
}

export interface IProject {
  id: string;
  title: string;
  image: string;
  cover: string;
  github: string;
  live: string;
}